services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_DB: mohassibe_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
